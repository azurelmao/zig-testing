Requires zig version `0.14.1`

Then do `zig build` and execute `./zig-out/bin/testing` in the project dir

TODOs:
- Add 3d block volume for finer raycast detection
- Change all [6]Elem arrays for std.EnumArray
- Deferred rendering?